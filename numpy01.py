# array uses similar datat types
# numpy stands for numerical python uses for mathematical operation such as  matrices and all.
import numpy as np
arr=np.array([1,2,3,4,5,6])
print(arr)
print(type(arr))
print(arr[1])

# we uses it for faster purposes in list we cannot perform the operation on two list altogether
# but in  numpy we can directly do it.

# 2d array
arr2=np.array([[[1,2,3],[11,12,13,14]],[[2,33,23,2],[10,11,12]]])
print(arr2)
print(arr2[0,1])
